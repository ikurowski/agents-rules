name: workflow-evaluator-acceptance
version: "1.0.0"
goal: Acceptance checks for evaluator output artifact.
steps:
  - id: commands-recorded
    description: Final artifact contains commands inventory.
    gate: command-record-complete
  - id: scorecard-labeled
    description: Final artifact includes overall score and label.
    gate: scorecard-label-complete
